<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Broadcast!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>


<body>

 <!-- Attribution: Nav bar from lecture code-->
 <nav class="navbar navbar-inverse">
  <%= if @current_user do %>
  <a class="navbar-brand" href="/broadcasts">Manager App</a>
  <% else %>

  <a class="navbar-brand" href="/users/new">Manager App</a>
  <% end %>
  <button class="navbar-toggler" type="button"
  data-toggle="collapse" data-target="#main-navbar">
  <span class="navbar-toggler-icon"></span>
  </button>

<%= if @current_user do %>
<div class="collapse navbar-collapse" id="main-navbar">
  <ul class="navbar-nav mr-auto">
    <li class="nav-item">
     <%= link "Dashboard", to: broadcast_path(@conn, :index), class: "nav-link" %>
   </li>
   <li class="nav-item">
     <%= link "Teams", to: team_path(@conn, :index), class: "nav-link" %>
   </li>
 </ul>
</div>


<span class="text-light">
  <strong><%= @current_user.first_name%> <%= @current_user.last_name %></strong> |
  <%= link("logout", to: session_path(@conn, :logout), method: :delete) %>
</span>
<li class="nav-item">
 <%= link "My Profile", to: user_path(@conn, :show, @current_user), class: "nav-link"%> </li>

 <% else %>
 <%= form_for @conn, session_path(@conn, :login), 
 [as: "user", class: "form-inline w-75"], fn _f -> %>
 <input type="text" placeholder="email" class="form-control" name="email">
 <input type="password" placeholder="password" class="form-control" name="password">
 <input type="submit" value="Log in" class="btn btn-primary">
 <% end %>
 <% end %>

</nav>

<div class="container">

  <div class="row">
    <div class="col-sm-12 my-2">
      <%= if get_flash(@conn, :info) do %> 
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %> 
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <% end %>
    </div>
  </div>

  <div class="row">
    <div class="col-sm-8">
      <%= render @view_module, @view_template, assigns %>
    </div>
  </div>

</div> <!-- container -->

<script src="<%= static_path(@conn, "/js/app.js") %>"></script>
</body>
</html>
